import { Box } from '@mui/material';
import MainContainer from '@/containers/Main/MainContainer';

const Main = () => {
	return (
		<Box sx={{ height: '100vh' }}>
			<MainContainer />
		</Box>
	);
};

export default Main;
